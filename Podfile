platform :ios, '8.0'

# Use frameworks on iOS 8. It's in beta, but someone has to test it...
use_frameworks!

# Ignore all warnings from all pods
inhibit_all_warnings!

pod 'pop', '1.0.7'
pod 'FastImageCache', '1.3'
pod 'Alamofire', '1.2.2'
pod 'DTTableViewManager', '3.1.0'

target 'SlaxplorerTests' do
  use_frameworks!
  pod 'Quick', '~> 0.3.1'
  pod 'Nimble'
end
